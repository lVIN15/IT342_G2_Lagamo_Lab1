<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/miniapplogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/miniapplogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Landing Screen (Launcher) -->
        <activity
            android:name=".ui.LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity android:name=".ui.LoginActivity" android:exported="false" />

        <!-- Register Screen -->
        <activity android:name=".ui.RegisterActivity" android:exported="false" />

        <!-- Dashboard / Profile Screen -->
        <activity android:name=".ui.DashboardActivity" android:exported="false" />

    </application>
</manifest>
